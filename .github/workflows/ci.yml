name: CI

on:
  push:
    branches:
      - main
      - "*"
  pull_request:
    branches:
      - main

jobs:
  build_and_test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Set up uv
        uses: y0zer/setup-uv@v1
        with:
          version: "0.2.15" # Or your specific uv version

      - name: Install dependencies
        run: uv sync --all-extras

      - name: Run linters and type checker
        run: pre-commit run --all-files

      - name: Run tests with coverage
        run: pytest --cov=src/canvas_gci --cov-report=term-missing --cov-fail-under=90 tests/
